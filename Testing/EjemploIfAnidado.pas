program Ejemplo;
var
a,b, n, s : integer;
p,q :boolean;
begin
    s:=0;
    n:=9;
    a:=10;
    b:=20;
    p:= true;
    q:= true;

    {if a>b then
        
        q := p and true}
        while s<=n do
            begin
            s := s+3;
            write(s)
            end
        
    {else
        if (a < 10) then
            q := true

        else
            q := false;
    write(q);}
end.
